---
component_skel:
  elk:
    belongs_to:
      - elk_all

container_skel:
  elk_container:
    belongs_to:
      - elk_containers
    contains:
      - elk

physical_skel:
  elk_containers:
    belongs_to:
      - all_containers
  elk_hosts:
    belongs_to:
      - hosts
